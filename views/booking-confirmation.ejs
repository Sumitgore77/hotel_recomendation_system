<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Booking Confirmation</title>
  <style>
    body {
  font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
  background: #f4f4f9;
  margin: 0;
  height: 100vh;
  display: flex;
  justify-content: center;
  align-items: center;
  overflow: hidden; /* 👈 prevents scrolling */
}

.ticket {
  background: white;
  border-radius: 16px;
  padding: 35px 45px;
  max-width: 700px;
  width: 100%;
  box-shadow: 0 12px 30px rgba(0, 0, 0, 0.15);
  border-left: 8px solid #1e2a38;
  transition: transform 0.3s ease, box-shadow 0.3s ease;
  max-height: 90vh; /* 👈 prevent overflowing out of view */
  overflow-y: auto;  /* 👈 scroll only if content is really too long */
}


    .ticket:hover {
      transform: translateY(-4px);
      box-shadow: 0 18px 45px rgba(0, 0, 0, 0.2);
    }

    h1 {
      color: #1e2a38;
      text-align: center;
      margin-bottom: 25px;
      font-size: 28px;
    }

    .section {
      margin-bottom: 30px;
    }

    .section h2 {
      color: #f8c291;
      font-size: 18px;
      margin-bottom: 10px;
      border-bottom: 1px dashed #ccc;
      padding-bottom: 6px;
    }

    .detail {
      margin-bottom: 10px;
      font-size: 16px;
    }

    .label {
      font-weight: bold;
      color: #333;
    }

    .btn {
      display: inline-block;
      margin-top: 30px;
      background-color: #1e2a38;
      color: #f8c291;
      padding: 12px 24px;
      border-radius: 8px;
      text-decoration: none;
      font-weight: bold;
      text-align: center;
      transition: background-color 0.3s, transform 0.2s;
    }

    .btn:hover {
      background-color: #2d3b50;
      color: #fff;
      transform: scale(1.03);
    }

  </style>
</head>
<body>
  <div class="ticket">
    <h1>✅ Booking Confirmed</h1>

    <div class="section">
      <h2>🏨 Hotel Information</h2>
      <div class="detail"><span class="label">Name:</span> <%= booking.hotel_name %></div>
      <div class="detail"><span class="label">Address:</span> <%= booking.hotel_address %></div>
      <div class="detail"><span class="label">Email:</span> <%= booking.hotel_email %></div>
      <% if (booking.contact) { %>
        <div class="detail"><span class="label">Contact:</span> <%= booking.contact %></div>
      <% } %>
    </div>

    <div class="section">
      <h2>📅 Booking Details</h2>
      <div class="detail"><span class="label">Booking Date:</span> <%= booking.booking_date %></div>
      <div class="detail"><span class="label">Check-in:</span> <%= booking.checkin_date %> at <%= booking.checkin_time %></div>
      <div class="detail"><span class="label">Check-out:</span> <%= booking.checkout_date %> at <%= booking.checkout_time %></div>
    </div>

    <a class="btn" href="/user/home">🏠 Back to Home</a>
  </div>
</body>
</html>
