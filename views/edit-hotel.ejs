<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Edit Hotel</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
  <div class="container mt-5">
    <h2>Edit Hotel</h2>
    <form action="/admin/update-hotel/<%= hotel.hotel_id %>" method="POST" enctype="multipart/form-data">
      <div class="mb-3">
        <label for="hotel_name" class="form-label">Hotel Name</label>
        <input type="text" name="hotel_name" id="hotel_name" class="form-control" value="<%= hotel.hotel_name %>" required>
      </div>
      <div class="mb-3">
        <label for="hotel_address" class="form-label">Hotel Address</label>
        <input type="text" name="hotel_address" id="hotel_address" class="form-control" value="<%= hotel.hotel_address %>" required>
      </div>
      <div class="mb-3">
        <label for="hotel_email" class="form-label">Hotel Email</label>
        <input type="email" name="hotel_email" id="hotel_email" class="form-control" value="<%= hotel.hotel_email %>" required>
      </div>
      <div class="mb-3">
        <label for="hotel_contact" class="form-label">Hotel Contact</label>
        <input type="text" name="hotel_contact" id="hotel_contact" class="form-control" value="<%= hotel.hotel_contact %>" required>
      </div>
      <div class="mb-3">
        <label for="image" class="form-label">Hotel Image</label>
        <input type="file" name="image" id="image" class="form-control">
        <% if (hotel.image) { %>
          <img src="/uploads/<%= hotel.image %>" width="150" height="100" class="mt-2">
        <% } %>
      </div>
      <button type="submit" class="btn btn-primary">Update Hotel</button>
      <a href="/dashboard?section=view-hotels" class="btn btn-secondary">Cancel</a>
    </form>
  </div>
</body>
</html>